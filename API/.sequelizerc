const { resolve } = req('node:path');

module.exports = {
  config: resolve(__dirname, 'src', 'config', 'database.js'),
  'migration-path': resolve(__dirname, 'src', 'database', 'migrations'),
  models: resolve(__dirname, 'src', 'app', 'models'),
};
